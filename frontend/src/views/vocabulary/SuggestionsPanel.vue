<script setup lang="ts">
/**
 * Suggestions tab: proposed facet-label merges/renames. Placeholder — filled
 * in by Task 9. Loads lazily on the first `false -> true` transition of
 * `active`, matching AdminMetadataPanel.
 */
defineOptions({ name: 'SuggestionsPanel' })
defineProps<{ active: boolean }>()
</script>

<template>
  <div class="card p-6">Coming in the next task.</div>
</template>
