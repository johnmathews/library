<script setup lang="ts">
/**
 * Facets tab: facet label management (values, counts, split colours).
 * Placeholder — filled in by Task 6. Loads lazily on the first
 * `false -> true` transition of `active`, matching AdminMetadataPanel.
 */
defineOptions({ name: 'FacetsPanel' })
defineProps<{ active: boolean }>()
</script>

<template>
  <div class="card p-6">Coming in the next task.</div>
</template>
