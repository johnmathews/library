# 1. 2026-06-11 — v0.1.0 build complete: all 17 work units shipped

## 1.1 What happened

The full greenfield build cycle for Library finished: 17 work units
(W1–W17) from `.engineering-team/runs/manual-20260610-154616/improvement-plan.md`,
executed over two days by the engineering-team workflow on branch
`eng-library-greenfield-build`. Per-unit journal entries exist alongside
this one; this entry records the cycle wrap-up.

## 1.2 End state

- **263 backend tests** (90% coverage) + **128 frontend unit tests** +
  Playwright e2e on desktop Chromium, iPhone and iPad WebKit viewports —
  all green, all enforced in CI (backend, frontend, e2e, compose-smoke,
  docker jobs).
- Full pipeline works end-to-end in the production compose stack:
  upload/consume/email/REST/MCP → routed OCR (scan-aware, `--redo-ocr`
  for iOS Notes embedded text) → Claude metadata extraction → Dutch +
  English FTS → GOV.UK-styled SPA served by the api image.
- paperless-ngx migration ready (`library import paperless --dry-run`).

## 1.3 Wrap-up findings (/done quality gate)

1. **Security scan:** no secrets, no key material, no sample-document
   PII in tracked files. One fix: a test fixture used John's real name
   and email — replaced with a synthetic identity before merge.
   `.gitignore` extended with `*.pem`, `*.key`, `credentials.json`.
2. **Code review (full-branch, dedicated reviewer):** all security
   seams clean (auth/CSRF, SPA catch-all traversal, file downloads and
   header injection, OCR subprocess args, SQL/FTS parameterization,
   the two sanctioned v-html sites). One merge-blocking robustness bug
   found and fixed test-first: a transient failure while queuing the
   thumbnail job after successful OCR would permanently mark the
   document `failed`; thumbnail deferral is now best-effort
   (`src/library/jobs.py`). Minor: logout `delete_cookie` now mirrors
   the set-cookie attributes.
3. **Known non-blocking items** (documented for future work): importer
   follows the paperless server's `next` pagination URL verbatim
   (trusted, operator-configured server; CLI-only); no Anthropic Batch
   API for import backfills (standard queue throttles cost); no
   semantic search yet (pgvector is the planned next phase).

## 1.4 Operational next steps for John

1. Deploy to the LXC per `docs/deployment.md` (clone → `.env` from
   `.env.example` with `LIBRARY_ANTHROPIC_API_KEY` → `docker compose up
   -d --build` → `library user add`).
2. Point Syncthing at the consume volume (or bind-mount over
   `/data/consume`; set `LIBRARY_CONSUME_FORCE_POLLING=true` on NFS/SMB).
3. Run the paperless migration: dry-run first, then live
   (`docs/migration.md`).
4. Walk the on-device iPhone/iPad checklist in `docs/frontend.md` §1.8.3.
